{
  "rules": {
    "by_username": {
      "$key": {
        ".read": "auth.uid === root.child('admin').val()",
        ".write": "auth.uid === root.child('admin').val()"
      }
    },
    "by_twitter_id": {
      "$key": {
        ".read": "auth.uid === root.child('admin').val()",
        ".write": "auth.uid === root.child('admin').val()"
      }
    }
  }
}